#!/bin/sh

echo "${0} ${*}" > /tmp/gm_sc.log

# shellcheck disable=SC2129

way-displays -s scaling off >> /tmp/gm_sc.log 2>&1

gamemoderun "${@}" >> /tmp/gm_sc.log 2>&1

way-displays -s scaling on >> /tmp/gm_sc.log 2>&1

