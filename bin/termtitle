#!/bin/zsh

# source this expensive script from a non login shell e.g. vim system call

if [ -f /usr/share/git/completion/git-prompt.sh ]; then
	. /usr/share/git/completion/git-prompt.sh
	git_ps1="$(__git_ps1)"
fi

print -Pn "%~${git_ps1}"

