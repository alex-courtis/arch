#!/bin/sh

while :; do
	turbostat --show PkgWatt --Summary --quiet --num_iterations 1 --interval 0.5 \
		| grep "^[0-9]" \
		| sed -e 's/\..*/W/g' \
		> "/tmp/cpu.power.new"
	mv "/tmp/cpu.power.new" "/tmp/cpu.power"
	sleep 3
done

