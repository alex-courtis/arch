#!/bin/sh

if [ $# -ne 2 ]; then
	exit
fi

export WS="${1}"
export DIRECTION="${2}"

FOCUSED=$(swaymsg -t get_workspaces | jq -r '.[] | select(.name == env.WS) | .focused')

if [ "${FOCUSED}" == "true" ]; then
	swaymsg -t command focus "${DIRECTION}"
else
	swaymsg -t command workspace "${WS}"
fi

