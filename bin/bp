#!/bin/zsh

echo "$#"

if [ $# -eq 0 ]; then
	/usr/bin/brave-origin --incognito --homepage "https://vimium.github.io/new-tab" >/dev/null 2>&1 &!
else
	/usr/bin/brave-origin --incognito "$@" >/dev/null 2>&1 &!
fi

