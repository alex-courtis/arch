events {
}

http {
    server {
        listen       localhost:8008;
        server_name  localhost;

        location / {
            root   /usr/share/nginx/html;
            index  blank.html;
        }
	}
}

