{
	"position": "top",

	"modules-left": ["river/tags", "river/layout", "river/window"],
	"modules-right": ["battery", "pulseaudio", "custom/slstatus", "cpu", "clock", "tray"],

	"fixed-center": false,

	"tray": {
		"spacing": 6
	},

	"river/tags": {
		"disable-click": true,
		"num-tags": 10
	},

	"river/window": {
		// truncates when it hits slstatus if a value is set
		"max-length": 999,
		"tooltip": false,
		"on-click": "wl-copy -p $(lswt -j | jq -r 'map(select(.activated)) | first | .title')"
	},

	"pulseaudio": {
		"tooltip": false,
		"states": {
			"full": 100,
			"mid": 99,
			"off": 0
		},
		"max-volume": 110,
		"format":       "{format_source} V {volume}%   ",
		"format-full":  "{format_source}   ",
		"format-mid":   "{format_source} V {volume}%   ",
		"format-off":   "{format_source}   ",
		"format-muted": "{format_source}   ",
		"format-source": "M {volume}%",
		"format-source-muted": "",
		"scroll-step": 5,
		"on-click": "volmute",
		"on-click-middle": "pavucontrol",
		"on-click-right": "volmutemic",
	},

	"battery": {
		"tooltip": false,
		"interval": 30,
		"states": {
			"low": 50,
			"critical": 25
		},
		"format": "",
		"format-charging": "{capacity}% ({time})   ",
		"format-discharging-low": "{capacity}% {time}   ",
		"format-discharging-critical": "{capacity}% {time} {power}W   ",
		"format-time": "{H}:{m}",
	},

	"custom/slstatus": {
		"exec": "slstatus -s",
		// extra space is always appended; newline?
		"format": "{}  ",
		"tooltip": false,
		"on-click-right": "bell"
	},

	"cpu": {
		"tooltip": false,
		"interval": 5,
		"format": "{load}   "
	},

	"clock": {
		"tooltip": false,
		"interval": 1,
		"format": "{:%a %d %b %H:%M:%S} ",
	}
}

// vim:ft=json5
