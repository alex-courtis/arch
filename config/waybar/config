{
	"position": "top",

    "modules-left": ["river/tags", "river/window"],
    "modules-right": ["custom/slstatus", "tray"],

	"fixed-center": false,

	"tray": {
		"spacing": 6
	},

	"river/tags": {
		"disable-click": true,
		"num-tags": 10
	},

	"river/window": {
		// truncates when it hits slstatus if a value is set
		"max-length": 999,
		"tooltip": false
	},

	"custom/slstatus": {
		"exec": "slstatus -s",
		"tooltip": false
	}
}

