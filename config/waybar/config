{
	"position": "top",

	"modules-left": ["river/tags", "river/layout", "river/window"],
	"modules-right": ["network", "pulseaudio", "battery", "custom/slstatus", "memory", "cpu", "clock", "tray"],

	"fixed-center": false,

	"tray": {
		"spacing": 6
	},

	"river/tags": {
		"disable-click": true,
		"num-tags": 10
	},

	"river/window": {
		// truncates when it hits slstatus if a value is set
		"max-length": 999,
		"tooltip": false,
		"on-click": "wl-copy -p $(lswt -j | jq -r 'map(select(.activated)) | first | .title')"
	},

	"network": {
		"interface": "wlp0s20f3",
		"format": "",
		"format-disconnected": "┃ {ifname} disconnected ",
	},

	"pulseaudio": {
		"tooltip": false,
		"states": {
			"full": 100,
			"mid": 99,
		},
		"format":       "{format_source}┃ V {volume}% ",
		"format-full":  "{format_source}",
		"format-mid":   "{format_source}┃ V {volume}% ",
		"format-muted": "{format_source}",
		"format-source": "┃ M {volume}% ",
		"format-source-muted": "",
		"scroll-step": 5,

		"on-click": "volmute",
		"on-click-right": "volmutemic",
		"on-scroll-up": "volraise",
		"on-scroll-down": "vollower",
		"on-click-middle": "pavucontrol",
	},

	"battery": {
		"tooltip": false,
		"interval": 30,
		"states": {
			"low": 50,
			"critical": 25
		},
		"format": "",
		"format-charging": "┃ {capacity}% ({time}) ",
		"format-discharging-low": "┃ {capacity}% {time} ",
		"format-discharging-critical": "┃ {capacity}% {time} {power}W ",
		"format-time": "{H}:{m}",

		"on-click": "volmute",
		"on-click-right": "volmutemic",
		"on-scroll-up": "volraise",
		"on-scroll-down": "vollower",
	},

	"custom/slstatus": {
		"exec": "slstatus -s",
		"tooltip": false,

		"on-click": "volmute",
		"on-click-right": "volmutemic",
		"on-scroll-up": "volraise",
		"on-scroll-down": "vollower",
	},

	"memory": {
		"tooltip": false,
		"interval": 5,
		"format": "┃ {percentage}% {swapPercentage}% ",
	},

	"cpu": {
		"tooltip": false,
		"interval": 5,
		"format": "┃ {load} ",
	},

	"clock": {
		"tooltip": false,
		"interval": 1,
		"format": "┃ {:%a %d %b %H:%M:%S} ",
	}
}

// vim:ft=json5
