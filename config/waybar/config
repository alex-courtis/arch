{
	"position": "top",

	"modules-left": ["river/tags", "river/layout", "river/window"],
	"modules-right": ["pulseaudio", "custom/slstatus", "tray"],

	"fixed-center": false,

	"tray": {
		"spacing": 6
	},

	"river/tags": {
		"disable-click": true,
		"num-tags": 10
	},

	"river/window": {
		// truncates when it hits slstatus if a value is set
		"max-length": 999,
		"tooltip": false,
		"on-click": "wl-copy -p $(lswt -j | jq -r 'map(select(.activated)) | first | .title')"
	},

	"custom/slstatus": {
		"exec": "slstatus -s",
		"tooltip": false,
		"on-click-right": "bell"
	},

	"pulseaudio": {
		"tooltip": false,
		"format": "{format_source} V {volume}%   ",
		"format-muted": "{format_source}   ",
		"format-source": "M {volume}%",
		"format-source-muted": "",
		"scroll-step": 5,
		"on-click": "volmute",
		"on-click-middle": "pavucontrol",
		"on-click-right": "volmutemic",
	}
}

// vim:ft=json5
