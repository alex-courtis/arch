{
	"position": "top",

    "modules-left": ["river/tags", "river/window"],
    "modules-right": ["custom/slstatus", "tray"],

	"fixed-center": false,

	"tray": {
		"spacing": 6
	},

	"river/tags": {
		"disable-click": true,
		"num-tags": 10
	},

	"river/window": {
		// truncates when it hits slstatus if a value is set
		"max-length": 999,
		"tooltip": false,
		"on-click": "wl-copy -p $(lswt -j | jq -r 'map(select(.activated)) | first | .title')"
	},

	"custom/slstatus": {
		"exec": "slstatus -s",
		"tooltip": false,
		"on-click-right": "pacmd play-file /usr/share/sounds/freedesktop/stereo/bell.oga @DEFAULT_SINK@"
	}
}

