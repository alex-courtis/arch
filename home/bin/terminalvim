#!/bin/sh

# Execute a terminal session with an interactive shell and run vim with the arguments supplied.
# Useful for xdg-open and open-in-your-editor browser extensions.

TMUX= term -e zsh --interactive -c "vim $*" &

