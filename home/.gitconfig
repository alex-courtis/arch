[alias]
	d = diff
	dn = diff --name-only
	ds = diff --staged
	co = checkout
	gup = smart-pull
	gm = smart-merge
	gl = smart-log
	s = status
[user]
	name = Alexander Courtis
	email = acourtis@atlassian.com
[core]
	excludesfile = ~/.gitignore
[push]
	default = current
[color]
	ui = auto

# only use these when attempting a particularly large operation
#[merge]
	#renameLimit = 9999
#[diff]
	#renameLimit = 9999
